language: csharp

os:
 - linux

dist: trusty

mono: none
dotnet: 2.0.0

before_install:
 - cd scripts
 - chmod +x *.sh
 - sudo apt-get update
 - sudo apt-get install binutils binutils-multiarch g++-multilib

script:
 - ./test.sh
